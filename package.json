{
  "name": "digital-visual-studio",
  "version": "2.0.0",
  "description": "Site vitrine Digital Visual Studio — By_MLP (Cloudflare Workers + D1 + KV)",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute dvs-database --remote --file=./migrations/0001_init.sql"
  },
  "devDependencies": {
    "wrangler": "^4.27.0"
  }
}
